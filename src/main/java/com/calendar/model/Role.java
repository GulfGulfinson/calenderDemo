package com.calendar.model;

public enum Role {
    ADMIN,      // kompletter zugang zu allen funktionen
    MANAGER,    // kann events erstellen/bearbeiten/löschen, kalender verwalten und so
    USER        // grundlegender zugang, kann persönliche events ansehen und erstellen
} 